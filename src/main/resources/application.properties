# ============================================================
# CONFIGURACIÓN BASE DE DATOS PostgreSQL
# ============================================================
# Reemplaza los valores con los de tu servidor
spring.datasource.url=jdbc:postgresql://newtest.accesspay.pro:8888/kelowna
spring.datasource.username=keyshell
# NOTA: Si la contrase\u00f1a contiene caracteres especiales (& # ^ etc), deben escaparse con \
# Ejemplo: & -> \&   # -> \#   = -> \=   : -> \:
spring.datasource.password=@7WY1y-Ard5q
spring.datasource.driver-class-name=org.postgresql.Driver

# Pool de conexiones (opcional pero recomendado)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000

# ============================================================
# CONFIGURACIÓN CORREO GMAIL
# ============================================================
# IMPORTANTE: NO uses tu contraseña de Gmail normal.
# Debes crear una "Contraseña de Aplicación" en:
#   myaccount.google.com → Seguridad → Contraseñas de Aplicaciones
# Esa clave tiene 16 caracteres, ejemplo: abcd efgh ijkl mnop
# Escríbela sin espacios: abcdefghijklmnop

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=alertaaccess@gmail.com
spring.mail.password=ctvqxmegijsdrjef
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============================================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ============================================================
# Nombre de la aplicación en los logs
spring.application.name=Invoice Monitor - Gastoncito

# Nivel de logs (INFO muestra los resultados, DEBUG muestra más detalle)
logging.level.com.monitor.invoices=INFO
logging.level.org.springframework.jdbc=WARN
logging.level.org.springframework.mail=INFO

# Archivo de log (opcional: también guarda logs en archivo)
logging.file.name=logs/invoice-monitor.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
